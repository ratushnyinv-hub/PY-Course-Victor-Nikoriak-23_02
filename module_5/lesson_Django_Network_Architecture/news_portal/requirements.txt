# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Залежності проєкту "Новинний портал"
# ─────────────────────────────────────────────────────────────────────────────
# Встановлення: pip install -r requirements.txt

# ── Веб-фреймворк ─────────────────────────────────────────────────────────────
Django==5.2               # Основний веб-фреймворк

# ── База даних ─────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9      # Адаптер PostgreSQL для Python
                            # (binary = не потребує компіляції)

# ── Змінні оточення ────────────────────────────────────────────────────────────
python-decouple==3.8        # Зчитує SECRET_KEY, DB_* з файлу .env
                            # Замість хардкодингу паролів у settings.py

# ── Продакшн WSGI сервер ───────────────────────────────────────────────────────
gunicorn==22.0.0            # Production WSGI сервер (замість вбудованого runserver)

# ── Статичні файли ─────────────────────────────────────────────────────────────
whitenoise==6.7.0           # Роздача CSS/JS/images безпосередньо через gunicorn
                            # Використовується в Dockerfile: CMD ["gunicorn", ...]

# ── Парсинг новин ─────────────────────────────────────────────────────────────
requests==2.32.3            # HTTP-запити для отримання сторінок rbc.ua
beautifulsoup4==4.12.3      # Парсинг HTML (витягуємо заголовки новин)
lxml==5.2.2                 # Швидкий парсер HTML (бекенд для BeautifulSoup)
