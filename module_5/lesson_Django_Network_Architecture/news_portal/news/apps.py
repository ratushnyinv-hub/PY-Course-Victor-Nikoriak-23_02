"""
apps.py — Конфігурація Django-додатку 'news'.

РОЛЬ У АРХІТЕКТУРІ:
    Цей файл описує метадані додатку та підключається до Django
    через INSTALLED_APPS у settings.py.

    Виконується ОДИН РАЗ при старті Django (не при кожному запиті).
    Основне призначення тут — підключити сигнали (signals) при потребі.

ВАЖЛИВО:
    НЕ писати тут бізнес-логіку або запити до БД!
    БД ще не ініціалізована в момент виклику ready().
"""

from django.apps import AppConfig


class NewsConfig(AppConfig):
    # Тип первинного ключа для нових моделей цього додатку
    default_auto_field = 'django.db.models.BigAutoField'

    # Ім'я пакету (директорії) додатку — має збігатись з назвою папки
    name = 'news'

    # Людська назва — відображається в адмін-панелі
    verbose_name = 'Новини'
