# ============================================================
# ⚠️  НАВМИСНО ПОГАНИЙ ФАЙЛ — демонстрація "тіньового імпорту"
#
# Цей файл названо  random.py  — так само, як стандартна
# бібліотека Python.  Через те, що поточний каталог
# перевіряється ПЕРШИМ у sys.path, цей файл "тінізує" (shadows)
# справжній  random  і Python ніколи до нього не дістанеться.
#
# ❌ НІКОЛИ не називайте свої файли іменами стандартних модулів!
#    random.py, math.py, sys.py, os.py, string.py тощо.
# ============================================================

MY_MESSAGE = "Я — підроблений random.py. Справжній модуль недоступний!"

# Тут НЕ MA є функції randint(), choice(), shuffle() тощо,
# тому будь-яка спроба  random.randint(...)  викине AttributeError.
